#include <stdio.h>
main()
{
	int d1, d2, d3, sum;
	float avg;

	printf("®”‚ğ3ŒÂ“ü—Í:");
	scanf("%d%d%d", &d1, &d2, &d3);

	sum = d1 + d2 + d3;
	avg = (float)sum / 3;

	printf("‡Œv=%d \n •½‹Ï=%.2f \n", sum, avg);
}